services:
  app:
    build: .
    container_name: hello-app
    expose:
      - "8000"

  nginx:
    image: nginx:alpine
    container_name: hello-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app